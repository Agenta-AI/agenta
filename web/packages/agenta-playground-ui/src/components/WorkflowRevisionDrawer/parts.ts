/**
 * `./drawer-parts` — the drawer's header and metadata panel, for stories and for callers that
 * embed a piece without the whole drawer. Kept off `./workflow-revision-drawer` so that entry
 * stays the drawer itself.
 */

export {default as DrawerHeader} from "./DrawerHeader"
export {default as MetadataSidebar} from "./MetadataSidebar"
export {default as DrawerContent} from "./DrawerContent"
