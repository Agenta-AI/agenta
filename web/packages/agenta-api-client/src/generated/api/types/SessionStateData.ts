// This file was auto-generated by Fern from our API Definition.

import type * as AgentaApi from "../index.js";

/**
 * Typed shape of the `data` column: the session's durable continuity state.
 *
 * Stored in the existing `data` JSON column (no dedicated columns) — every field is
 * read and compared in the runner, never queried server-side, so a typed DTO gives the
 * contract without a schema change.
 */
export interface SessionStateData {
    /** The latest-run harness's agentSessionId; a fast-path mirror of harness_sessions[<latest harness>].agent_session_id. */
    latest_agent_session_id?: (string | null) | undefined;
    /** Conversation-level turn counter compared against each harness's turn_index. */
    latest_turn_index?: (number | null) | undefined;
    /** Per-harness resume state, keyed by harness id (e.g. 'claude', 'pi'). Durable mirror of the staleness guard. */
    harness_sessions?: (Record<string, AgentaApi.HarnessSessionRecord | null> | null) | undefined;
}
