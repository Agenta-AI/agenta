/**
 * `cn` re-exported from the design system, so components moved out of the app keep the import
 * they had. The implementation is `@agenta/ui`'s — there is no second merge strategy here.
 */
export {cn} from "@agenta/ui/ui"
