/**
 * How every host names the templates page.
 *
 * The toolbar frame leaves the title and subtitle to the host's own page header, so the words
 * would otherwise be written once per app and drift. They live beside the gallery instead.
 */
export const TEMPLATE_GALLERY_COPY = {
    title: "Templates",
    subtitle:
        "Start from a proven agent — review what it does, connect what it needs, and open the playground.",
    searchPlaceholder: "Search templates…",
} as const
