/**
 * Auto-refresh cadence in ms. The progress bar under "auto-refresh" fills over exactly this
 * window, so the constant and the animation must stay one value.
 */
export const AUTO_REFRESH_INTERVAL = 15000
