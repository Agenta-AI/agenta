// Legacy column-fetch atom kept for backward compatibility.
// The backend now returns column metadata directly; the atom is retained to
// prevent runtime errors when older bundles expect the symbol.
export {}
