// The panel moved to @agenta/chat (mobile's composer drills into it too); this path stays for
// the Storybook story that still imports it.
export {PermissionsPickerPanel as default} from "@agenta/chat/components"
