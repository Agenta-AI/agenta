export interface GenerationComparisonCompletionOutputProps {
    rowId: string
    focusDisable?: boolean
    variantId: string
    variantIndex?: number
    isLastRow?: boolean
    isLastVariant?: boolean
}
