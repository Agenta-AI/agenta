/** Stable scope id for the audit-log paginated table. */
export const AUDIT_LOG_SCOPE_ID = "audit-log"

/** Page size for the audit-log InfiniteVirtualTable. */
export const AUDIT_LOG_PAGE_SIZE = 50

/** Fixed row height (px) for the audit-log table. */
export const AUDIT_LOG_ROW_HEIGHT = 40
