import {PageTitle, type PageTitleProps} from "@agenta/ui/page-title"

// Compat shim: the component lives in @agenta/ui so mobile and desktop cannot drift apart.
export {PageTitle}
export type {PageTitleProps}

export default PageTitle
