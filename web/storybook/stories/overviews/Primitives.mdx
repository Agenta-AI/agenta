import {Meta} from "@storybook/addon-docs/blocks"

<Meta title="@agenta/ui/Primitives/Overview" />

# Primitives

Primitives are the **antd replacements** — one migrated `@agenta/ui` component per antd
control, each rendered beside its antd reference and pixel-diffed by the VRT gate. Geometry
comes from the shared `controlScale` tokens and colors from antd semantic tokens, so light
and dark both track the app.

They are grouped by antd family:

- **Forms** — data-entry controls (Button, Input, Select, Checkbox, Radio, Switch, …).
- **Overlays** — floating surfaces (Dialog, Sheet, Tooltip, Dropdown, AlertDialog).
- **Display** — read-only presentation (Badge, Avatar, Alert, Divider, Collapse, Tabs, …).
- **Feedback** — loading / progress (Skeleton, Spinner, Progress).

Open each subfolder's **Overview** for its component table and conventions.
