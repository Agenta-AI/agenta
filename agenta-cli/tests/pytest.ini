[pytest]
asyncio_default_fixture_loop_scope = session
markers = 
    asyncio: mark a test as an async test
    variant_management: mark test as an SDK variant management test


[tool.pytest.ini_options]
asyncio_mode = "auto"
