# Minimal package

This single-agent package omits setup instructions, setup notes, permissions, skills, connections, automations, and workspace entries. It demonstrates that those fields are optional. The model and other creation settings come from ordinary agent creation.

Validate its extension against [the single-agent schema](../extension.schema.json). Runtime implementation remains NOT IMPLEMENTED.
