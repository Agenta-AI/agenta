# Future multi-agent example

UPDATE: The version-one example is now [the single-agent package](../current/README.md). This retained two-agent fixture describes deferred behavior only: NOT IMPLEMENTED.

Validate its shape against [the future extension schema](../../../../../../../../docs/design/agent-workflows/projects/agent-plugin-templates/future-extension.schema.json). Both agents own their descriptions. References use optional tool_description for calling guidance and omit permission overrides. Graph validation does not prove runtime support.
