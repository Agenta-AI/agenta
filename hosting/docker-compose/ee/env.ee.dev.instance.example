# Example environment file for running a second Agenta instance in parallel
#
# Usage:
#   1. Copy your main env file: cp .env.ee.dev.local .env.ee.dev.instance2
#   2. Edit the copy to change the values below
#   3. Run with:
#      COMPOSE_PROJECT_NAME=agenta-ee-dev-instance2 ./hosting/docker-compose/run.sh \
#        --license ee --dev --env-file .env.ee.dev.instance2
#
# The COMPOSE_PROJECT_NAME ensures containers, networks, and volumes are isolated.
#
# ============================================================================
# VARIABLES TO CHANGE FOR SECOND INSTANCE
# ============================================================================
#
# These are the only variables you need to modify. Change the port numbers
# to avoid conflicts with the main instance (which uses 80, 8080, 5432).
#
# Recommended port offset: add 9000 to each port
#   - Main instance: 80, 8080, 5432
#   - Instance 2:    9080, 17080, 14432
#
# ----------------------------------------------------------------------------

# URLs - change port from 80 to 9080 (or your chosen port)
AGENTA_WEB_URL=http://localhost:9080
AGENTA_API_URL=http://localhost:9080/api
AGENTA_SERVICES_URL=http://localhost:9080/services
AGENTA_API_INTERNAL_URL=http://api:8000

# Ports - these are used by docker-compose to map host ports
TRAEFIK_PORT=9080
TRAEFIK_UI_PORT=17080
POSTGRES_PORT=14432

# ============================================================================
# Copy all other variables from your main .env file below this line
# ============================================================================

