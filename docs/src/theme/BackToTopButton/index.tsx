import type {ReactNode} from 'react';

/**
 * The theme renders a floating "back to top" button on every doc page with no
 * option to turn it off; this stub removes it.
 */
export default function BackToTopButton(): ReactNode {
  return null;
}
