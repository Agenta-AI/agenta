# Agentic Organization

This folder reframes the earlier `agentic-engineering` work at the organization level.

Current structure:

- `misc/` = prior exploratory and transitional notes
- `product-os/`
- `gtm-os/`
- `system-os/`
- `sre-os/`
- `os_process_state_and_location_working_note.md` = working note on OS process vs state, `agent-os`, project state, and provisional placement

Each OS currently starts with:

- `layer.md` = scope, interfaces, overlaps, distinctions
- `process.md` = main loops, activities, roles, skills, agents
- `state.md` = knowledge, state, artifacts, and feedback surfaces
