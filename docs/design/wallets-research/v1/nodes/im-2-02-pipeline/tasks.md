# IM-2-02 tasks

1. Merge `IM-2-01`, `WP-2-03` and `WP-2-04` in the IM worktrees, gateway side first.
2. Walk the ten checks and record each result.
3. Deploy locally with `AGENTA_WALLETS_ENABLED=true` and run the acceptance module against it.
4. Run the same deployment with the flag off and confirm a relay behaves as it does on `main`.
5. Write `nodes/im-2-02-pipeline/acceptance.md` in the shape Wave 1's has: the procedure, the
   commands, what a failure of each would mean, and the results of the first run — including
   whatever the first run found, which is the part of Wave 1's document that earned its keep.
6. Record the counts for every suite, and declare checkpoint 2 or say plainly why not.
