# IM-1-01 tasks

1. Merge `WP-1-01` and `WP-1-02` in the IM worktree.
2. Walk the eleven checks in the specification; record the result of each.
3. Compare the published debit message against the settlement posting DTO field by field. A
   mismatch here is the failure this node exists to catch.
4. Run both migrations forward and back by hand against a local EE Postgres.
5. Run the unit and integration suites for both packages.
6. Read the concurrency test rather than trusting that it passed.
7. Record reviewed inputs and results before releasing `WP-1-03` to fork.
