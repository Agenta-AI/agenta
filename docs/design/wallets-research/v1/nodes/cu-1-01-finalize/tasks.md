# CU-1-01 tasks

1. Remove superseded terminology and dead configuration the delivered code exposed.
2. Update `entities.md` with the delivered table, column and stream names, and mark the §5 questions
   Wave 1 answered.
3. Update `wave-1.md` with the actual migration ids, stream names, consumer groups and `MAXLEN`
   values.
4. Update `open-designs.md`: close only the items Wave 1 genuinely decided, and say what closed them.
5. Update `preflight.md` dispositions with anything the wave changed.
6. Run `ruff format`, `ruff check`, the full API suites, and `git diff --check`.
7. Hand the branch and the written acceptance procedure to the user for local deployment and the
   acceptance run.
