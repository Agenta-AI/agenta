# Wave 1 cleanup nodes

| Node | Depends on | Purpose |
| --- | --- | --- |
| `CU-1-01` | `IM-1-02` | Remove superseded names/configuration, synchronize the design documents with delivered names and limits, and run the final repository-level checks before local deployment. |

`CU-1-01` is a real node with its own worktree and throwaway branch; it is not informal post-merge work.
