## 1. Work and evidence

- [x] 1.1 Review each baseline requirement against the pinned branch and source-map.md.
  - Evidence: a cold spec review of the wallet diff at `884c966fe4` found all 13 baseline scenarios matched the code (2 with test gaps, since closed). The fixes that followed changed behaviour under items 15, 20, 21 and 22, and the spec was updated to those decisions. A final Codex review of the whole wallet diff (`origin/main...6f4e567f9d -- api`) found no P0 or P1. Every finding and its disposition: ../../../review-findings.md. Every code-versus-design difference: ../../../spec-divergences.md.
- [x] 1.2 Rerun focused foundation tests and deployment acceptance when implementation verification is authorized; record exact revisions and zero-skipped infrastructure evidence.
  - Tests, done at `6f4e567f9d` (`0693b307c1` changes docs only): concurrency test 1 passed; `ee/tests/pytest/unit` 573 passed, 0 skipped; `ee/tests/pytest/integration/wallets` plus `integration/measurements` 43 passed, 0 skipped; `oss/tests/pytest/unit` without infrastructure addresses 6804 passed, 119 skipped, the same as `main`.
  - Deployment acceptance, IM-1-02 sections 0 to 8, on a disposable EE stack with the flag on. Run 1 at `144c0dec91`: sections 0, 1 and 3 to 8 passed, with exact expected amounts (1071 musd LLM, 50 musd MCP), one debit after replay, no reprocessing after a restart, and the poisoned entry dead-lettered. Section 2 failed only against a wrong claim in the procedure (admin-created organizations have no row; lazy provisioning covers them, open-designs item 14); the procedure is corrected.
  - Run 2 at `0693b307c1`, the final head, covers all of sections 0 to 8: migrations at `ee0000000005` and `ee0000000002`; both wallet workers running; `check()` provisioning a missing row and refusing at floor 0; `settle()` provisioning a missing row and booking 1071 musd of deficit; the fake LLM and MCP paths (1071 and 50 musd); one debit after a replay; no reprocessing after a worker restart; a poisoned entry dead-lettered and listed. The signup-grant backfill was idempotent (dry run 3 eligible, apply 3 awarded, second dry run 0 eligible).
  - Run 3 at `0693b307c1`: plan changes through Stripe test mode. A real Checkout subscription (Hobby to Pro), a switch up to Business, a switch down to Pro, and a cancel from the billing page. Each transition's general-balance change equalled the incoming allowance minus the clawback, exactly to the musd, across all four transitions (1,000,000 to 5,999,992 to 50,998,916 to 6,001,243 to 1,001,622 musd).
- [ ] 1.3 Resolve review requirements and obtain explicit merge authorization separately.
  - Review requirements: every finding has a disposition in ../../../review-findings.md. Merge authorization: not given. Only the owner gives it.
- [ ] 1.4 Archive this documentation baseline only when its target branch and verified implementation status are explicitly recorded.
