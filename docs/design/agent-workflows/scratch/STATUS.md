# STATUS — moved

The PR #4936 follow-up work now lives in one folder, one file per thread:

`docs/design/agent-workflows/scratch/pr-4936-followup/` — start at `README.md`.

Each thread has Context, Explanations, History, and Open decision threads (where we
collaborate).
