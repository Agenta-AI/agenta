{
  "schema_version": 1,
  "entry": "outbound",
  "agents": {
    "outbound": {
      "name": "Outbound Prospecting",
      "instructions": "./ai.agenta/agents/outbound/AGENTS.md",
      "setup": "./ai.agenta/agents/outbound/SETUP.md",
      "configuration": {
        "runner": {
          "permissions": {
            "default": "allow_reads"
          }
        }
      },
      "skills": ["prospect-research"],
      "connections": [
        {
          "key": "mailbox",
          "required": false,
          "purpose": "Prepare outreach drafts in a selected mailbox.",
          "options": [
            {
              "kind": "gateway",
              "provider": "composio",
              "integration": "gmail"
            },
            {
              "kind": "mcp",
              "server": "mail-drafts",
              "credential_requirements": [
                {
                  "kind": "header",
                  "name": "Authorization",
                  "key": "mail-drafts-authorization",
                  "description": "Authorization header for the selected mail MCP server."
                }
              ]
            }
          ],
          "policy": {
            "permissions": {
              "default": "ask"
            }
          },
          "setup_notes": "Ask whether mailbox access is useful. Let the user choose Gmail or the declared MCP server. If skipped, save Markdown drafts."
        }
      ],
      "subagents": [
        {
          "agent": "researcher",
          "description": "Research prospective companies and return source-backed facts.",
          "permission": "ask",
          "setup_notes": "Verify one read-only company research example after the target profile is known."
        }
      ],
      "automations": [
        {
          "key": "weekday-prospects",
          "name": "Weekday prospect research",
          "required": false,
          "trigger": {
            "type": "schedule",
            "schedule": "0 9 * * 1-5"
          },
          "inputs_fields": {
            "messages": [
              {
                "role": "user",
                "content": "Research prospects from the saved target profile. Prepare drafts only."
              }
            ]
          },
          "setup_notes": "Offer this after the user approves a sample. Confirm the time and timezone before activation."
        }
      ],
      "workspace": {
        "entries": [
          {
            "type": "file",
            "source": "./ai.agenta/agents/outbound/files/target-profile.md",
            "path": "target-profile.md",
            "setup_notes": "Replace the unknown values after the user confirms the target profile."
          },
          {
            "type": "directory",
            "path": "reports"
          }
        ]
      }
    },
    "researcher": {
      "name": "Prospect Researcher",
      "instructions": "./ai.agenta/agents/researcher/AGENTS.md",
      "setup": "./ai.agenta/agents/researcher/SETUP.md",
      "configuration": {
        "runner": {
          "permissions": {
            "default": "allow_reads"
          }
        }
      },
      "skills": ["prospect-research"],
      "connections": [
        {
          "key": "web-search",
          "required": true,
          "purpose": "Find public company information and source URLs.",
          "options": [
            {
              "kind": "gateway",
              "provider": "composio",
              "integration": "composio_search"
            }
          ],
          "policy": {
            "permissions": {
              "default": "inherit"
            }
          },
          "setup_notes": "Bind an existing search connection when available and verify a source-backed result."
        }
      ],
      "subagents": [
        {
          "agent": "outbound",
          "description": "Hand a completed research brief back to the outbound coordinator when Researcher starts an independent task.",
          "permission": "ask",
          "setup_notes": "This mutual link is valid. A synchronous outbound to researcher to outbound call must be rejected by the runtime cycle guard."
        }
      ],
      "automations": [],
      "workspace": {
        "entries": [
          {
            "type": "directory",
            "path": "research"
          }
        ]
      }
    }
  }
}
