# Final AI QA sweep — decisions log

This log records judgment calls made during the autonomous sweep without stopping for user
approval. The goal is transparency: any judgment that changes scope, skips a cell, changes
a config, or treats a near-pass as a pass is recorded here so the user can review and
reverse it.

A decision is reversible when the affected cell can be re-run with the original setup at
low cost. A decision is not reversible when it affects a vault credential, a committed
agent config, or a dependency that takes time to recreate.

---

| Date       | ID    | Decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Why                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Reversible?                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ---------- | ----- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-06-25 | D-001 | Deleted the orphan `services/agent/skills/agenta-getting-started/SKILL.md` (single source of truth = the catalog) instead of wiring it in.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | The runner materializes skills from the wire into per-run temp dirs and never reads that file path; grep confirmed no code references it. The LIVE getting-started body is `api/oss/src/core/workflows/platform_catalog.py` (`_GETTING_STARTED_BODY`). Wiring the orphan in would change the served skill body, so deleting the dead, diverged copy is the safe reconcile.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Yes — restore the file from git history if a code path is ever added that reads it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 2026-06-25 | D-002 | Reconciled the overclaiming comment on `AGENTA_FORCED_TOOLS` in `sdks/python/agenta/sdk/agents/adapters/agenta_builtins.py`; deferred making the forced-tools mechanism actually deliver builtins over the wire.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | The old comment claimed read+bash are forced "because Pi only renders skills when read is available," but the runner never consumes `request.tools` / `builtin_names` to grant builtins — Pi gets `read`/`bash` from its own DEFAULTS, so skills render regardless of this list. Comment-only fix now; wiring the forced set to deliver builtins on the wire is deferred (it works via Pi defaults today).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Yes — the deferred wiring is a tracked follow-up; the comment can be revised when it lands.                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2026-06-25 | D-003 | **Correction of D-001 — it was WRONG.** Restored `services/agent/skills/agenta-getting-started/SKILL.md` (byte-exact from `9b24931b32^`). D-001 deleted it as a "dead file," but the dir is REQUIRED at build time: both `services/agent/docker/Dockerfile:43` and `Dockerfile.dev:32` do `COPY skills ./skills`, which fails when the path is missing — the deletion broke the `agenta-sandbox-agent` image build on big-agents. The EE/OSS dev compose also bind-mounts `services/agent/skills:/app/skills`, so a missing host dir empties the mount. Restored, then proved the prod build now passes the COPY step and completes (image `agenta-sandbox-agent-buildtest`), and the rebuilt dev image + recreated `:8280` sidecar is healthy with the skill present.                                                                                                                                                                                                                                                                                                                                                                                                            | What D-001 got RIGHT and is NOT reversed: the served getting-started skill BODY at runtime comes from the Python catalog (`_GETTING_STARTED_BODY` in `api/oss/src/core/workflows/platform_catalog.py`, slug `_agenta.agenta-getting-started`), injected over the `/run` wire via the embed; the runner materializes `request.skills` into per-run temp dirs and never reads the baked dir. So the baked file is dead-at-runtime but required-at-build. The deeper reconcile (the baked placeholder has diverged from the catalog body — collapse to one source, e.g. generate the dir from the catalog at build time or drop the COPY and stop bind-mounting) is a DEFER-TODO; do NOT re-delete the dir without first removing the Dockerfile COPY and the dev-compose bind-mounts in the same change.                                                                                                                                                                                                                                                                                                                                          | No — the file is back in git; reversing this re-breaks the build.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 2026-06-25 | D-004 | MCP Finding 1 (Daytona loopback): added an `isDaytona` guard to `buildSessionMcpServers` so the INTERNAL gateway-tool channel (loopback `127.0.0.1` HTTP MCP) is SKIPPED on Daytona; gateway tools are delivered via the existing file relay there. Threaded `isDaytona` from the run plan through the engine. Scoped the guard to the internal channel ONLY — a user http MCP (Layer 2) is a remote url and stays delivered on Daytona.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | The internal channel's URL is a runner-host loopback; on Daytona the harness runs IN the sandbox, so that URL resolves to the sandbox's own loopback and is unreachable → Claude+gateway+Daytona could call no tool. The file relay (`startToolRelay` via `sandboxRelayHost`) already works on Daytona, so dropping only the unreachable advertisement (not the execution path) is the #4844-decided fix ("HTTP advertisement for local, file relay for Daytona"; gateway-tool-mcp open question 3).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Yes — revert the guard; behavior returns to advertising the (unreachable) loopback URL.                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 2026-06-25 | D-005 | MCP Finding 2 (F-032 Pi user-MCP silent drop): chose to FAIL LOUD (refuse) rather than support Pi user MCP. New `PI_USER_MCP_UNSUPPORTED_MESSAGE` + an up-front `run-plan.ts` gate refusing ANY user MCP (stdio AND http) on a Pi harness, placed before the harness-agnostic stdio gate so Pi gets the clearer reason for both transports.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Pi delivers tools through its bundled extension, not ACP MCP, so `buildSessionMcpServers` returns `[]` for Pi and a user http MCP was dropped with no log and an HTTP 200. Pi user MCP is not a supported path (http MCP is a Claude-only capability), so a loud refusal up front (mirroring the stdio/code gates) is correct rather than building new Pi MCP support.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Yes — relax the gate if Pi user MCP support is ever added.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2026-06-25 | D-006 | MCP Finding 3 (SSRF): added `validateUserMcpUrl` in `mcp.ts` — require `https`, reject loopback / link-local (incl. `169.254.169.254` metadata) / private literals, with an opt-out env allowlist `AGENTA_AGENT_MCP_HOST_ALLOWLIST`. Chose a scheme + host-literal guard rather than full DNS-resolution pinning.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | The runner attaches Agenta-resolved named secrets as headers to the author-supplied url, a classic SSRF/exfil sink. The capability is flag-gated off by default (`AGENTA_AGENT_ENABLE_MCP`) and config-trust, so a scheme + internal-host guard is proportionate; DNS-rebinding pinning would be over-engineering for a trusted-config, off-by-default feature (noted as a possible later hardening).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Yes — guard is one function; tighten (DNS pinning) or loosen (allowlist) without wire change.                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 2026-06-25 | D-007 | HITL Finding 1 (over-approval by tool NAME, HIGH/Codex): scoped a stored approval decision to the SPECIFIC call instead of the tool name. The cross-turn lookup (`responder.ts` `permissionRequestKeys` / `extractApprovalDecisions`) now keys a decision by `toolCallId` (precise/warm) AND by `approvalKey(name, args)` — name bound to a canonical hash of the args — and DROPS the bare-name key entirely. Resume still works because a cold-replayed call re-derives the same name + args; a different call to the same tool has different args and re-prompts. Precision and resume DO coexist (no escalation needed).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Bare-name keying was a HITL bypass: an `allow` on `edit({path:'a.txt'})` auto-approved any later `edit({...})`, including a different/sensitive path, on a fresh tool-call id. The name-fallback existed for cold-replay resume (the cold model mints a fresh ACP tool-call id each turn, so the id no longer matches — see `continuation.test.ts` / `buildTurnText`), so it could not simply be removed; binding it to args keeps resume while closing the hole. The args are available on both sides: the live ACP `ToolCallUpdate` carries `rawInput` (+ name via `title`/`kind`), and the stored side recovers name+args from the correlated `tool_call` block.                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Yes — the keying lives in two pure functions in `responder.ts`; revert restores name-fallback. **NOTE/cross-layer:** the SDK ingress (`adapters/vercel/messages.py`, a different lane) does not currently emit the `{approved}` envelope for a verbatim `approval-responded` tool part, so live resume depends on whatever decision shape reaches `extractApprovalDecisions`; the responder is now correct + precise regardless, and the resume property is pinned at the responder layer by unit tests (id-resume AND fresh-id name+args resume). |
| 2026-06-25 | D-008 | HITL Finding 2 (F-026 Approve/Deny disabled ~70-140s, reads as a hang): ROOT-CAUSED to the FE gating the buttons on the conversation-level `busy` (`status === submitted/streaming`). Fixed by enabling Approve/Deny as soon as the `approval-requested` part renders — removed the `disabled={busy}` gate in `ToolPart.tsx` (and the now-dead `busy` prop threaded through `AgentMessage` + both callers); replaced with a local `responding` guard against a double-submit.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | An approval request can only appear WHILE the stream is in flight (that is how the `interaction_request` / `tool-approval-request` event arrives), so `busy` is necessarily true when the prompt first shows; gating on it disabled the buttons for the whole remaining turn — exactly the 70-140s observed. The AI SDK is built for mid-stream answers: `addToolApprovalResponse` records the decision immediately and only auto-resends once `status` is not streaming/submitted (verified in `@ai-sdk/...` source), so clicking mid-stream is safe and the resume timing is unchanged. NOT a backend-park latency.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Yes — FE-only change across 4 files in `AgentChatSlice/`; restore `disabled={busy}` to revert.                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2026-06-25 | D-009 | **F-031 alias fix.** A bare Claude alias (`haiku`/`sonnet`/`opus` + `[1m]`) or a dated `claude-*` id now resolves to the `anthropic` provider in the SDK connection resolver instead of failing F-017's "needs a provider prefix" rule, and the missing-provider hint names the harness-reachable provider (`anthropic/<m>` on Claude, not `openai/<m>`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | F-017 (PR #4846) made the documented Claude `model:"haiku"` alias return a 500 with a wrong `openai/haiku` hint. The alias set is reused from `capabilities.py` (`CLAUDE_MODEL_ALIASES`, the `/inspect` surface) so the two never drift; the harness is threaded from `RuntimeAuthContext.harness` already on the resolve path. Inference only fills a MISSING provider — an explicit `provider` is always honored — and a bare alias now behaves exactly like the documented `anthropic/haiku` form (reaches auth), so behavior is consistent, not widened.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Yes — revert the resolver helper + the `MissingProviderError(hint_provider=...)` arg.                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-06-25 | D-010 | **F-030 error-on-span: local Pi gets a standalone `agent_error` span, not a stamp on Pi's own span.** For Claude/gateway/Daytona the runner owns the agent span and `recordError` stamps it directly; for local Pi (the runner's sandbox-agent otel is span-less and Pi's extension span lives in another process, already flushed) `recordError` emits a separate `agent_error` span under the caller's traceparent.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Stamping Pi's own span from the runner is impossible (different process, already ended). A standalone child span on the same trace is the only way the error reaches the `/invoke` trace for local Pi without a cross-process protocol. (F-029 skills DO reach Pi's own span because they are passed via the new `AGENTA_SKILLS_LOADED` env the extension reads at `agent_start`, before its span ends.)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Yes — error/skills additions are additive trace attributes; remove the env var + `recordError` to revert.                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2026-06-25 | D-011 | **F-029 forced/platform skills need no special casing.** The trace stamps `ag.agent.skills.loaded`/`.count` from the materialized skill names, which already include the forced `_agenta.*` platform skills.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | The forced platform skills ride `AgentConfig.skills` → `wire_skills()` → `request.skills`, so by the time the runner materializes `plan.skillDirs` the platform skills are in the list. No separate "forced set" plumbing is needed; the post-materialization names are the accurate "actually loaded" set.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Yes — additive attribute.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2026-06-25 | D-012 | **Error sanitize is at the Python boundary, not a second TS pass.** `wire.result_from_wire` and the `ts_runner` transport errors now route through `sanitize_runner_error` / `_transport_error`: first line only, stack-frame/path leak stripped, capped to 300 chars, full detail logged (never shown). The runner's `conciseError` is left as-is.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `conciseError` already trims the KNOWN auth/credit cases to clean lines (don't double-handle — a clean message passes through unchanged); the leak is the fall-through case + the transport `RuntimeError`s that dump `response.text`/`stderr`/`stdout`. Both the streaming (`streaming.py`) and one-shot (`adapters/sandbox_agent.py`) paths funnel through `result_from_wire`, so one boundary covers both.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Yes — single helper; inline back to the raw `f"...{error}"` to revert.                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2026-06-25 | D-013 | **Extends D-007 (Codex xhigh round-2 hardening of the HITL over-approval fix).** Three more over-approval paths Codex found, all fixed in `responder.ts`: (1) **HITL `allow` -> `once`, never `always`** — `decisionToReply` no longer prefers `always`; a per-call approval must not become a turn-wide grant that skips re-gating later calls (the orchestration/permission tests now assert `once`). (2) **Dropped the historical `toolCallId` key from `extractApprovalDecisions`** — the `/messages` path is always cold-replay (fresh session/id per turn), so a stored replayed id can never legitimately match the fresh re-raised id and keeping it only risked an args-blind match on an id collision; the cross-turn anchor is now name+args exclusively. (3) **`approvalKey` normalizes absent args to `{}`** (so a genuine no-arg tool still resumes) and **fails closed** (no key -> re-prompt) for non-JSON args (bigint/cycle/NaN/Infinity/Date/Map) instead of throwing or collapsing to `{}`.                                                                                                                                                                   | D-007 closed the bare-name bypass but Codex showed `always` re-opened a turn-scoped version of it, the retained id key was a latent args-blind match, and the fail-closed-on-absent-args broke no-arg-tool resume while `name#null` collisions remained. The `once` mapping is functionally identical for headless auto-allow (each call is gated) and strictly safer for HITL; reliable `rawInput`/`tool_call.input` capture on both sides makes the `{}` normalization safe (a with-args call keys differently).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Yes — all in `responder.ts`; revert each of the three independently. **Codex Finding 3 (live E2E resume on slice Track B / SDK ingress) remains the cross-layer NOTE in D-007 — out of these files.**                                                                                                                                                                                                                                                                                                                                              |
| 2026-06-25 | D-015 | **F-036 namespace wrinkle: moved the skill + error trace attributes to recognized `ag.*` namespaces instead of registering new ones in the api schema.** Skills: `ag.agent.skills.{loaded,count}` → `ag.meta.skills.{loaded,count}`; errors: `ag.error.{message,provider}` → `ag.exception.{message,provider}` (runner `tracing/otel.ts` only; NO api schema change).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Agenta's OTel ingest (`api/oss/src/core/tracing/utils/attributes.py` `initialize_ag_attributes`) strict-whitelists top-level `ag.*` keys against the `AgAttributes` pydantic model and relocates any unrecognized key (e.g. `ag.agent.*`, `ag.error.*`) to `ag.unsupported.*`. `ag.meta` (free-form `Meta`) is already the recognized home for run/request metadata (model/system/response — see `vercel-ai-adapter/plan.md`), and `ag.exception` (free-form `Data`) is the recognized error bucket. Both pass ingest validation untouched, so reusing them lands the data first-class with zero schema/SDK change and zero ingest risk. Registering `ag.agent.*`/`ag.error.*` would require coordinated edits to the SDK pydantic model + the api validator + new tests + back-compat — non-trivial for a pure-naming polish where the info is already present, so it was NOT done.                                                                                                                                                                                                                                                            | Yes — three attribute-name changes in one file; revert restores the `ag.unsupported.*` behavior.                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2026-06-25 | D-016 | **F-036 builtins-in-loaded: REPORTED as a server-side root cause, NOT fixed in the runner — supersedes D-011's assumption.** D-011 claimed the forced `_agenta.*` platform skills always ride `request.skills` so the trace shows them with no special casing. Live F-036 disproved that: on a `pi_agenta` run with a CUSTOM config the `loaded` list held only the author `qa-skill`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | The platform default skill (`_agenta.agenta-getting-started`) is `@ag.embed`-ed only into the DEFAULT agent config template (`services/oss/src/agent/schemas.py` `_DEFAULT_AGENT_CONFIG`); a custom config (no embed) drops it, so it never reaches `request.skills`, and the runner — which has no independent forced-skill installation and treats `pi_agenta` like `pi_core` for skills — cannot stamp a skill it never received. The runner trace stamping is therefore CORRECT (it shows all skills it got). Force-injecting the platform skill for every `pi_agenta` run regardless of config is the "separate workstream" noted in `harnesses.py`/`agenta_builtins.py` and lives in the Python service/harness layer, outside this lane's `otel.ts` boundary. Reported up for the orchestrator rather than reaching across files.                                                                                                                                                                                                                                                                                                        | N/A — no code change made for this part; it is a finding.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2026-06-25 | D-018 | **Codex LOW-5 (runner HTTP transport has no auth header):** added a per-call `_runner_auth_headers()` in `sdks/.../utils/ts_runner.py` that reads `AGENTA_AGENT_RUNNER_TOKEN` and sends `Authorization: Bearer <token>` on `deliver_http` + `deliver_http_stream` when set; unset = no header. (IDs D-014..D-016 were taken by a concurrent lane; using D-018+.)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | The runner already implements the matching opt-in gate (`server.ts`, default OFF, accepts `Authorization: Bearer`/`X-Agenta-Runner-Token`). With the gate on, an un-tokened POST from the co-located Python service 401s — a one-sided footgun. Reusing the SAME env var name keeps one config knob for both sides; default-off behavior is unchanged on loopback. Read fresh per call, not cached at import.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Yes — single helper + two header spreads; drop them to revert.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2026-06-25 | D-019 | **Codex LOW-6 (enforcement default mismatch):** changed `run-plan.ts` from `enforcement === "strict"` to `enforcement !== "best_effort"`, so an OMITTED `enforcement` is treated as strict.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | The Python wire schema (`WireSandboxPermission.enforcement`) defaults to `"strict"`, but the runner treated only the literal `"strict"` as strict — an omitted value silently became best_effort for a direct runner caller. The Python service always fills `"strict"`, so the LIVE path is unchanged; this only aligns a direct caller with the documented default. Added two run-plan unit tests (omitted→rejected like strict; explicit best_effort→allowed).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Yes — one comparison flip; revert restores the literal-strict check.                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2026-06-25 | D-020 | **Codex LOW-7 (dead `prompt` wire field):** REMOVED `prompt` from the TS `AgentRunRequest` (`protocol.ts`) and dropped its branch in `resolvePromptText` (now derives the latest turn from `messages` only).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | The Python producer never emits `prompt` (a `test_wire_contract.py` test asserts `"prompt" not in payload`), `WireRunRequest` lacks it, and it was absent from `KNOWN_REQUEST_KEYS` + the goldens on BOTH sides — dead on the wire. `resolvePromptText` stays (transcript/run-plan use it) but only as the messages-derived latest-turn helper. Goldens + both wire-contract tests stayed intact. Converted ~38 runner test fixtures from the `prompt:` convenience field to `messages: [{role:"user",...}]`; the otel `start({prompt})` calls use a SEPARATE inline type and were untouched.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Yes — re-add the field + its branch; but it re-introduces the documented producer/runner asymmetry.                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 2026-06-25 | D-021 | **F-028 (QA matrix says skills are n/a on pi):** corrected validity rule 4 + the capability×harness rows so skill cells are `valid` on BOTH `pi` and `agenta` (not `n/a` on pi).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Live 2026-06-25 proved author skills load+invoke on plain `pi_core` (token present), because both Pi-family harnesses drive the same `pi` ACP agent and the runner materializes skills for it. The real `agenta`-vs-`pi` difference is the FORCED `read`+`bash` set (which surfaces the skill), not skill support. Historical run-result rows recording the discrepancy left as-is (they document the find).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Yes — docs-only wording; revert the rule-4 + matrix-row edits.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2026-06-25 | D-014 | **Daytona sandbox-leak fix (credit-burner). Two-part backstop, runner-only.** (1) **Server-side TTL backstop** in `provider.ts`: the Daytona create object now sets `autoStopInterval` (env `SANDBOX_AGENT_DAYTONA_AUTOSTOP_MINUTES`, default `15` min, clamped `>= 1`) alongside the existing `ephemeral: true`. (2) **Signal handler** in `server.ts` (`registerShutdownHandler`): `SIGTERM`/`SIGINT` best-effort deletes any in-flight sandbox(es) before exit, drained from a new in-flight registry in `sandbox_agent.ts` (`destroyInFlightSandboxes`, registered after `startSandboxAgent`, removed in the `finally`); timeout-bounded (5 s race) and idempotent so it can never hang shutdown.                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | The per-run `finally` deletes the sandbox on normal/error/client-disconnect, but a process KILL (docker stop/SIGTERM/SIGKILL/OOM mid-run) skips it → leak. AND there was NO server-side backstop: the SDK wrapper hardcodes `autoStopInterval: 0` (auto-stop OFF) while `ephemeral` only auto-deletes ON STOP, so they cancel out and a leaked sandbox never self-reaps (10 found leaked). The wrapper spreads our create object AFTER its hardcode, so our non-zero `autoStopInterval` wins; an idle leaked sandbox then stops and the ephemeral auto-delete fires. `0` clamps to the default precisely because `0` would re-disable auto-stop and reintroduce the leak. The signal handler covers graceful `docker stop` (where a signal IS delivered); the auto-stop covers SIGKILL/OOM (where it is not). Chose `15` min (the Daytona SDK's own documented default) as a sensible idle backstop comfortably beyond a normal run — `autoStopInterval` measures IDLE time, and an actively prompting sandbox is busy, so it does not cut live runs short. Resources (cpu/mem/disk) left untouched (snapshot-baked in `build_snapshot.py:81`). | Yes — `provider.ts` (set `autoStopInterval` back to `0` / remove the field) + `server.ts` (`registerShutdownHandler`) + `sandbox_agent.ts` (registry) revert independently; all additive, no `/run` wire change.                                                                                                                                                                                                                                                                                                                                   |
| 2026-06-25 | D-022 | **F-036 HITL Deny dead-end FIXED — FE-driven deny resume (the decision asked for in F-036), closing the D-007 cross-layer NOTE.** Two layers, both in this lane's files. (1) **SDK ingress (`adapters/vercel/messages.py`):** `_tool_part_blocks` now translates a verbatim `approval-responded` (or terminal `output-denied`) tool part into the `{approved: bool}` `tool_result` envelope keyed by `toolCallId` — the SAME envelope the standalone `tool-approval-response` part produces — so the runner's `extractApprovalDecisions` actually receives the decision (approve OR deny). New `_approval_decision` helper reads `approval.approved`, falling back to `output-denied` = denied. (2) **FE resume predicate (`@agenta/playground` `agentShouldResumeAfterApproval`, used by both `AgentChatPanel.tsx` + `AgentChatConversation.tsx` as `sendAutomaticallyWhen`):** an explicit, unit-tested predicate that resumes on approve AND deny (replaces the stock `lastAssistantMessageIsCompleteWithApprovalResponses`). Picked FE-driven deny-resume (model continues after deny→reject→tool-error) over a silent terminal short-circuit, per the F-036 decision prompt. | The verbatim UIMessage (Track A, production) path posts the approval INLINE on the `tool-<name>` part (`state: approval-responded`, `approval.approved`); the old ingress routed that to `_tool_part_blocks`, which only emitted a `tool_call` (no decision) for any non-output state — so the runner NEVER learned the decision, re-parked the gate, and a deny dead-ended in `approval-responded` (F-036). The runner side was already correct (D-007/D-013 `extractApprovalDecisions` reads `{approved}` envelopes, `HITLResponder` maps deny→reject→tool-error→continue, egress emits `tool-output-denied`); the only missing piece was the ingress emitting the envelope for the inline shape. The stock SDK predicate already returns true for a deny-only turn (verified against the installed `ai` source), but wrapping it in a named, tested predicate pins the deny-resume contract at the FE seam rather than relying on SDK internals.                                                                                                                                                                                             | Yes — ingress: revert `_tool_part_blocks`'s `else` branch + drop `_approval_decision`. FE: re-point both `sendAutomaticallyWhen` to `lastAssistantMessageIsCompleteWithApprovalResponses` and delete `agentApprovalResume.ts`. No `/run` wire change (the `{approved}` envelope already existed for the standalone-part path). Tests: Python `test_ui_messages.py` +5 (inline deny/approve/output-denied/pending + standalone deny); FE `agentApprovalResume.test.ts` +9 (resend-on-deny is the headline).                                         |
| 2026-06-25 | D-023 | **F-033 dev-overlay guard (FE-only, dev-mode UX nit).** Softened both `useChat` `onError` handlers to `console.warn(... rendered in-chat ...)` and attached `.catch(ignoreStreamRejection)` to every floating `sendMessage` / `regenerate` call in `AgentChatPanel.tsx` + `AgentChatConversation.tsx`, so an aborted/errored agent stream no longer bubbles an unhandled rejection to the Next.js dev Runtime Error overlay. The in-chat `error` alert (the `error` from `useChat`) is unchanged and remains the user-facing surface.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | F-033: a missing-key (Gemini) stream error correctly rendered the in-chat alert AND tripped the dev-only Runtime Error overlay (`new Promise <anonymous>` stack, `[AgentChatPanel] useChat error`). The floating promise call sites had no rejection handler; production has no dev overlay so the alert was already the only prod surface. Kept the in-chat rendering (the content side was already PASS); only suppressed the unhandled-rejection footgun.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Yes — restore `console.error` and remove the `.catch(ignoreStreamRejection)` + the helper to revert; in-chat rendering is unaffected either way.                                                                                                                                                                                                                                                                                                                                                                                                   |

---

## How to add an entry

When you make a judgment call, append a row:

- **Date**: ISO date, e.g. `2026-06-25`.
- **ID**: `D-NNN` in sequence.
- **Decision**: what you decided (one sentence, concrete).
- **Why**: the reasoning that drove it (one sentence; cite the relevant cell ID if applicable).
- **Reversible?**: Yes / No / Partial — and what reversal requires if not trivial.

Do not edit or delete prior rows. If a decision was wrong, add a new row noting the
correction and referencing the original `D-NNN`.
