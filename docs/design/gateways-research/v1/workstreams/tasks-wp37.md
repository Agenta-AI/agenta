# WP37 tasks — Production Agenta builtin MCP provider

- [ ] Agree and document the first Agenta-owned MCP tool set and its authorization model.
- [ ] Implement the production server and adapter; remove production dependence on the mock
      adapter while retaining the explicit development mock provider.
- [ ] Add **unit and integration tests** for discovery, allowed calls, denied calls, and disabled
      mock mode.
- [ ] Add **OSS/EE gateway and harness acceptance** proving a real Agenta-owned tool call.
- [ ] Verify the runner’s internal `agenta-tools` channel remains separate from this public
      builtin provider.
