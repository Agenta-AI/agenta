# WP31 tasks — MCP OAuth consent and registration flow

- [ ] Add dashboard/API connection and scope-selection flow with **unit tests** for state and
      callback validation.
- [ ] Persist and display ready, needs-auth, and needs-input endpoint states; cover their API and
      dashboard integration.
- [ ] Implement automatic client-registration fallback with unit and local-provider integration
      coverage.
- [ ] Add browser-driven **OSS and EE acceptance coverage** for consent, callback, reconnect,
      scope step-up, and fallback.
