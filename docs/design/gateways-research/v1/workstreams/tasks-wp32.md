# WP32 tasks — Bedrock/Vertex endpoint base URL validation and coverage

- [ ] Specify and implement deployment-specific `base_url` validation.
- [ ] Add Bedrock and Vertex endpoint fixtures with explicit custom hosts/prefixes.
- [ ] Cover every supported request door and static rewrite route.
- [ ] Add negative validation and no-fallback regression tests.
