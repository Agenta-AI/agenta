# MCP execution status

Updated: 2026-09-15.

Product scope is confirmed. This commit records the handoff and target UX; it does not implement the UX, run application QA or establish release readiness.

| Work | Status | Evidence / next action |
| --- | --- | --- |
| Product decisions and handoff | Documented | Read release-decisions-2026-09-15.md and MCP-RELEASE-HANDOFF.md. |
| Current branch baseline and independent flags | Baseline recorded; independent enablement BLOCKED | Head 6df148cb39, base main 236619ebb768. Baseline suites run; no failure is a regression from this PR. No product flag exists for either gateway plane on any layer. The LLM plane cannot be disabled: `sdks/python/agenta/sdk/agents/platform/connections.py` replaced `GET /secrets/` with `POST /gateways/llms/resolve` and raises rather than falling back (`connections.py:1106-1109`), so the non-gateway model path was deleted, not gated. Needs a product decision before dependent work packages proceed. Wallets confirmed absent here (they are on feat/add-wallets at fef84e9118). |
| Multiple accounts and migration | Pending | Implement platform-consistent connection identity. |
| URL-first Connect flow | Pending | Implement mcp-connection-ux.md. |
| Runner per-tool permission wiring | Pending validation | Prove complete flow, then implement. |
| Tools and permissions on a real provider | Blocked, no evidence | Read-only discovery against the two connected real providers was attempted and could not run. Both endpoints belong to a single project whose only member is a personal account; that project and its organization hold no API key, current or deleted, and keys are stored hashed, so no existing credential can be used. The relay route authorizes `USE_MCP_ENDPOINTS` on the calling scope, and the gateway exposes no admin-scoped or tools-refresh route to stand in. Nothing was minted and no tool was called. The outbound leg is fine: from the API container both MCP servers answer the expected 401 challenge and the Axiom authorization-server metadata returns 200, so only a caller-side credential is missing. Unblock by re-consenting both providers into a QA-owned project once the tunnel returns, which also yields the product-visible tool list and permission editor this row needs. |
| Audit and recovery | Pending | Small fields and failure tests. |
| Automated tests and recorded QA | Pending | Fill mcp-release-gate.md evidence. |
| Astra medium / CodeRabbit feedback loops | Codex runnable; CodeRabbit has a mechanism | Codex gpt-6-astra at medium reasoning is available on this box and verified by a smoke run. CodeRabbit skipped this PR at 518 of 300 files and reported the skip as a passing check. Path-filter-excluded files provably do not count toward the limit (525 changed minus 7 filtered equals the 518 it counted), and CodeRabbit reads config from the head branch, so a temporary `path_filters` block on feat/add-gateways scopes to this PR alone. Two passes: source plus infra (232 files), then tests (about 165). Remove the block before merge. |
| Real-provider OAuth registration | Proven on two providers | The client-resolution fix (`508ea1a024`, 13:42 local) precedes the first successful registrations: Axiom and Linear OAuth clients and grants were created at 13:58-13:59 local, and their endpoint rows, added at 13:26-13:27, had failed consent before it. The commit records that no `oauth_provider` row existed at commit time, so these are the first ever. The Axiom client is keyed on the authorization server rather than the resource, which is the fix's shape. Registration and consent only: both endpoints still carry `tools = {}` and no gateway connections exist, so discovery to policy to execution is still unproven. |
| MCP OAuth consent end-to-end | Passing against the mock; unproven in a browser | `acceptance/gateways/test_mcp_gateway_oauth_consent_acceptance.py` now passes on the demo stack against the direct address, and the whole `acceptance/gateways/` directory is green at 43 passed. Both earlier obstacles are fixed. The fixture failure was a cookie-scheme mismatch: SuperTokens marks its session cookie `Secure` whenever `AGENTA_API_URL` is https, which is every tunnelled stack, and `http.cookiejar` will not send such a cookie over plain http, so the suite could not hold a session against its own stack (`e0365826d8`). The published-address mismatch is fixed by `AGENTA_MOCK_MCP_GATEWAY_PUBLIC_URL`, which makes the mock issuer publish one chosen base in every document it serves (`9190854e7d`). What remains unproven is the leg no test covers: a real browser opening the consent page on a publicly resolvable issuer. That is blocked on the tunnel row below, not on code. |
| Mock-backed OAuth suites in CI | Not run in CI | Both `acceptance/gateways/test_mcp_gateway_oauth_acceptance.py` and `..._consent_acceptance.py` skip unless `AGENTA_GATEWAYS_MOCKS_ENABLED` is true, and they need the `mock-llm-gateway` and `mock-mcp-gateway` containers. Nothing under `.github/` sets that variable or names either container, and no workflow runs `docker compose`, `run.sh` or `test.sh` at all. The only job running the api acceptance layer is in `44-railway-tests.yml`, and it points `AGENTA_API_URL` at a deployed Railway preview rather than starting a stack; the mock services are defined only in the two dev compose files, not under `hosting/railway/`. So setting the flag there would convert two silent skips into two hard failures, because `mock-mcp-gateway:9092` does not resolve in a Railway container and `localhost:9092` does not exist on the runner. The suites run locally and on the demo stack, through `hosting/docker-compose/test.sh`, which exports the flag and the non-secret mock token itself for a `--dev` stage. Closing the gap needs a new job that builds the dev API image the mocks run from, brings a dev compose stack up inside the runner, and then calls that script: a full stack per pull request, not a change to an existing workflow. Deliberately deferred; it is not a blocker for this release. |
| MCP release gate | NOT READY | Stop only when all mandatory evidence passes. Current external blockers: the demo stack's ngrok tunnel is down at account quota, so no consent flow can complete; the Anthropic key has no credit and the mounted Claude login is empty, so no real Claude run is possible; and the LLM-routing premise above needs a decision. The internal-traffic half of the tunnel outage is fixed on the stack: the services container now reaches the API in-network and the baked services address is host-reachable, which returned the services suite to 338 passed / 0 failed and the four evaluation acceptance tests to green. That services address must go back to the tunnel URL once ngrok returns. |
| LLM SDK and wallet work | Later | Not required to implement for MCP readiness. |

## Decision history

Mahmoud confirmed project-wide multiple accounts, stable slugs/IDs, editable names, configured selection, our own MCP gateway, deferred infrastructure work and phased release. He clarified that LiteLLM SDK applies only to LLM evaluation. He requested the integration-style MCP Connect flow, auditability in this PR and autonomous work through the release-ready gate with Astra medium and CodeRabbit feedback loops.

Name suggestions and authentication discovery use fallbacks because arbitrary server metadata is not guaranteed. No runtime proof has yet validated the target UX.

## Getting this pull request reviewed by CodeRabbit

CodeRabbit refuses a review above 300 changed files and reports the refusal as a passing check, so its green mark on this pull request means nothing was read. The cap is a platform limit rather than a setting, and neither a plan change nor `@coderabbitai full review` gets past it, because the count is checked before the review runs.

Files excluded by path filters are not counted. CodeRabbit's own skip comment listed seven files ignored by the organization's filters and then reported 518 files selected against 525 changed, which is the subtraction made visible. CodeRabbit also reads this configuration from the branch under review, so filters committed here scope to this pull request and change nothing for any other one. `inheritance: true` merges arrays child-first, so the organization's exclusions still apply on top.

The review therefore runs in two passes, each well under the cap:

| Pass | Contents | Files |
| --- | --- | --- |
| 1 | Production source and infrastructure. The `path_filters` block now in `.coderabbit.yaml` | 259 |
| 2 | Tests. Swap that block for the inverse list and request another review | 179 |

Counts measured at the revision that added this note, with the organization's inherited exclusions applied, against base `236619ebb768`. The branch is still growing and gained three source files during the few minutes this note was being written, so re-measure before each pass rather than trusting these numbers; pass 1 had 41 files of headroom when written. If a later pass approaches the cap, move infrastructure and configuration into pass 2 before splitting anything.

Run each pass with `@coderabbitai full review` after pushing, and confirm the skip banner is gone before reading the findings. Design-doc markdown under `docs/design/` is in neither pass: it is this team's working notes, not shipped code.

**The block must be deleted before this pull request merges.** Left in place it would narrow reviews for every future pull request in the repository. Confirm `git diff main -- .coderabbit.yaml` is empty before merging. Note also that CodeRabbit stored a learning on 2026-09-12 advising against repository-wide path filters for this pull request; that advice predates the branch-scoped approach described here and should be removed from its learnings so it does not argue against the fix.
