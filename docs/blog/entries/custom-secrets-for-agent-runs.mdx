---
title: "Attach secrets and credentials to agents"
slug: custom-secrets-for-agent-runs
date: 2026-09-09
tags: [v0.115.3]
description: "Attach API keys and credentials to AI agents in Agenta without pasting them into chat. Create or select a project secret and bind it to an environment variable."
---

<Summary>

You can now give an agent credentials and secret without sharing the value in chat. The credentials are saved in an encrypted vault, they can be used by the agent but not read or modified by it.

To give your agent access to a new credential, go to advanced, then either create or attach a pre-existing secret. The agent will get access to it in the next turn.

Additionally, whenever the agent needs a secret it will prompt to create a new secret and attach it to its session.

</Summary>

{/* truncate */}

You can now give an agent credentials without pasting them into chat. Create a secret or attach one you've already saved, then let the agent use it for the task.

## Attach a credential

Open Advanced in your agent's configuration, then Custom secrets → Attach. Choose an existing secret or click Create new.

Set the environment variable the tool expects, such as SERVICE_API_TOKEN, and attach it. The agent can use the credential on its next turn. You can refer to the variable in your instructions without sharing the value in chat.

## Add a secret when the agent asks

If the agent needs a credential during a task, it can ask you to configure one. Click Configure on the request card, then create or select the secret and attach it. Don't paste the credential into your reply.

The saved value can't be read back through the product. Code running for the agent can access an attached credential to use it, so only attach the credentials it needs. Removing an attachment doesn't revoke the credential at its provider.
