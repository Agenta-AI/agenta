---
title: "Give agents custom secrets without exposing their values"
slug: custom-secrets-for-agent-runs
date: 2026-09-16
tags: [v0.116.0]
description: "Create a write-only project secret, attach it to an agent revision, and inject only the selected secret into its run."
---

<Summary>

Give an agent the secret it needs without revealing the value.

Create a write-only project secret, attach it to an agent revision, and inject only the selected secret into its run.

</Summary>

{/* truncate */}

## What you can do

Create a write-only project secret, attach it to an agent revision, and inject only the selected secret into its run.

## How it works

Create a project secret, attach it to the agent revision, and select it for the run. The value remains write-only in the product.

## Good to know

Secret values are write-only. Availability and permissions depend on environment and project access.
