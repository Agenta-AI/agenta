1. Create a new environment and install the requirements and activate:
```bash
uv venv -n custom_workflows
source custom_workflows/bin/activate
uv pip install -r requirements.txt
```

2. Start the servers for each app