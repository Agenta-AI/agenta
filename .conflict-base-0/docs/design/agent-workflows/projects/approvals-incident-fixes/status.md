# Status

2026-07-19: Planning complete, awaiting review.

- Research R1 through R8 closed with code evidence; findings in `research.md`. Four open
  risks are listed at the end of that file; the history-fingerprint question (risk 1) and
  the cross-stack file sharing (risk 2) are the two that need attention during
  implementation, and both have prescribed mitigations in `plan.md`.
- Plan written as five independently landable steps in `plan.md`, targeted at an
  implementer (GPT-5.6 Sol / Codex) who has not read the incident conversation.
- QA script written in `qa.md`, including the MP4 recording requirement and the
  release-gate cells to re-run.
- Nothing has been implemented, committed, or pushed from this planning session. The dev
  `session_turns` wipe (plan step 1c) has NOT been run.

Next action: Mahmoud reviews this workspace; on approval, hand `plan.md` to the
implementation agent, step 1 first.
