interface:
  display_name: "Scan Codebase"
  short_description: "Orchestrate CR and QA scans"
  default_prompt: "Use $scan-codebase with depth=deep to run independent CR and QA scans of this branch."

policy:
  allow_implicit_invocation: true
